<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_lnd_41v_1z">
 <title>Installation - eXist-db</title>
 <conbody>
  <p>Für die Benutzung von ediarum wird eine eXist-db Installation benötigt. Die letzte Version läßt
   sich <xref href="http://exist-db.org/exist/apps/homepage/index.html#subscriptions" format="html"
    scope="external">hier</xref> herunterladen. Vor der Installations müssen die <xref
    href="http://exist-db.org/exist/apps/doc/quickstart.xml#system-requirements" format="html"
    scope="external">Installationsvoraussetzungen</xref> erfüllt sein. Die genauen
   Installationsschritte finden sich in der Online-Dokumenation. Es kann auch <xref href="http://www.bbaw.de/telota/software/sade"
    format="html" scope="external">SADE</xref> benutzt werden, eine angepasste und erweiterte
   Version von eXist.</p>
  <p>uf Linux lässt sich eXist mit dem Befehl
   <codeph>java -jar eXist-{version}.jar -console</codeph> installieren. Als <b>target
    path</b> gibt man etwa den <b>vollständigen</b> Pfad zum Unterverzeichnis
    <b>/pfad/zu/exist_db</b> an und als <b>Data dir</b> den wieder <b>vollständigen</b> Unterordner
    <b>/pfad/zu/data</b>. Daraufhin wird man aufgefordert das Admin-Passwort zu setzen. Die
   Voreinstellungen zum maximalen Speicher (1024 MB) und zum Cache (128 MB) kann man stehen
   lassen.</p>
  <p>Vor dem Starten sollte noch der Port angepasst werden (s<xref
    href="konfig_datenbank.dita#concept_k5t_ybv_1z"/>). Über die Konsole kann eXist-db dann
   gestartet werden etwa mit dem Befehl: <codeph>nohup bin/startup.sh $</codeph> Die Datenbank lässt
   sich nach dem Starten über den Browser unter<xref href="http://telotadev.bbaw.de:9121/exist/"
    format="html" scope="external">server:port/exist</xref> erreichen.</p>
  <p>
   <div>
    <p>Ist die Datenbank korrupt oder soll sie auf null zurückgesetzt werden, d. h. alle Daten,
     Benutzerkonten (inkl. des <b>admin</b>) und installierte Apps gelöscht werden, reicht es nach
     dem Stoppen der Datenbank den separaten Daten-Ordner auf dem Server zu löschen. Mit einem
     erneuten Starten der Datenbank erhält man eine leere Datenbank und kann dort wieder neue Apps
     installieren oder Daten einspielen. Zuerst sollte man allerdings das <b>admin</b>-Passwort neu
     setzen.</p>
   </div>
  </p>
 </conbody>
</concept>
